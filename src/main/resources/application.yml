
spring:
  application:
    name: "Tanks-Game"
  datasource:
    url: jdbc:postgresql://localhost:5432/tanks_game
    username: gameApp
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

server:
  port:
    9099
